{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Base capability set for the Jarvis Nebula desktop shell. Stage 0 keeps this minimal — the health probe only. Stage 1 expands it for filesystem, dialog, and shell capabilities.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "core:event:default",
    "dialog:default",
    "shell:default"
  ]
}
